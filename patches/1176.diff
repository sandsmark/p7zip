diff --git a/CPP/Build.mak b/CPP/Build.mak
index 22b8e16..f53286b 100644
--- a/CPP/Build.mak
+++ b/CPP/Build.mak
@@ -82,14 +82,16 @@ Hunk #5, a/CPP/Build.mak LFLAGS = $(LFLAGS) /LARGEADDRESSAWARE
 !IFDEF DEF_FILE
 LFLAGS = $(LFLAGS) -DLL -DEF:$(DEF_FILE)
 !ELSE
-# !IF "$(CPU)" != "ARM"
+!IF defined(MY_FIXED) && "$(PLATFORM)" != "arm" && "$(PLATFORM)" != "arm64"
 LFLAGS = $(LFLAGS) /FIXED
-# !ENDIF
+!ELSE
+LFLAGS = $(LFLAGS) /FIXED:NO
+!ENDIF
 # /BASE:0x400000
 !ENDIF
 
 
-# !IF "$(CPU)" == "AMD64"
+# !IF "$(PLATFORM)" == "x64"
 
 !IFDEF SUB_SYS_VER
 
